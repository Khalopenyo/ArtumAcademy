-- =====================================================================
-- Artum Academy: rich-контент урока (текст + картинки + видео)
--
-- Урок перестаёт быть «только видео»: добавляем HTML-контент, который
-- редактируется визуальным редактором (TipTap) и показывается санитизированным.
-- Видео (video_url) остаётся, но становится необязательным.
--
-- Аддитивно: новая nullable-колонка, ничего не ломает.
-- Бакет Storage `lesson-content` создаётся приложением при первой загрузке
-- картинки (admin.storage.createBucket, public) — отдельной DDL не требует.
-- =====================================================================

ALTER TABLE public.lessons ADD COLUMN IF NOT EXISTS content TEXT;
