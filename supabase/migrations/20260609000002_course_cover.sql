-- =====================================================================
-- Artum Academy: обложка курса (картинка / GIF / видео)
--
-- Курс получает медиа-обложку, которая показывается в карточке каталога
-- и в баннере страницы курса. Если обложки нет — фолбэк на градиент
-- (cover_gradient). Файл хранится в Supabase Storage (bucket course-covers).
--
-- Аддитивно: новая nullable-колонка, ничего не ломает.
-- =====================================================================

ALTER TABLE public.courses ADD COLUMN IF NOT EXISTS cover_url TEXT;
