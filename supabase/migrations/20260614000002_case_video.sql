-- =====================================================================
-- Artum Academy: ссылка на видео в кейсе
--
-- Тяжёлые работы (видео) логичнее показывать по ссылке с видеохостинга
-- (RuTube / YouTube / VK Video / Vimeo), а не заливать файл. Если ссылка
-- задана — в карточке кейса показывается встроенный плеер вместо фото.
--
-- Аддитивно: новая nullable-колонка, ничего не ломает.
-- =====================================================================

ALTER TABLE public.cases ADD COLUMN IF NOT EXISTS video_url TEXT;
